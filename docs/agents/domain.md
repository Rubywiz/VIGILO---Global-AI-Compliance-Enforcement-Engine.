# Domain Docs

This repository uses a **Single-context** layout.

- Domain language and project context are located at `CONTEXT.md` in the repository root.
- Architectural Decision Records (ADRs) are located in `docs/adr/` in the repository root.
